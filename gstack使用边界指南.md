# gstack 在「炒股学堂」项目中的使用边界

## 一、gstack 是什么

gstack 是一个**工程评审层**，它在 Claude Code 中以 slash command 的方式运行。它的职责是在关键节点对你的产品方案、技术设计做结构化评审，产出测试计划和任务拆解，帮助你**在动手写代码之前想清楚**。

它不是项目管理工具，不是 CI/CD，不是代码生成器。

---

## 二、什么时候**必须用** gstack

### 1. 项目启动前 — 产品方案评审（已完成）

> 已执行：`/plan-eng-review 项目文档.md`

在产品方案写完后、第一行代码写之前执行。gstack 会通读你的产品文档，对以下维度做系统性质疑和校验：

- 技术栈选型是否匹配需求
- 架构设计是否有结构性缺陷
- MVP 范围是否合理（是否过大/过小）
- 数据源是否可靠
- 测试策略是否覆盖核心路径
- 是否有遗漏的关键决策

**产出物：**

- 测试计划（`.gstack/projects/项目开发2/xxx-test-plan.md`）
- 实施任务列表（`.gstack/projects/项目开发2/tasks-xxx.jsonl`）
- 10+ 项架构决策确认

**触发条件：** 产品文档完成，准备进入编码阶段。

---

### 2. MVP 完成后 → V2 启动前 — 增量评审

当 MVP 开发完成，准备进入 V2（事件复盘 + 个性化 + 形态库扩展到 8-12 种）时，建议对 V2 的方案再做一次评审。

**触发条件：** 有新一版产品方案/技术方案文档需要被挑战。

---

### 3. 架构/技术栈发生重大变更时

如果实施过程中出现以下情况，应该再做一次评审：

- 决定更换数据源（如 AKShare → 其他）
- 决定换掉某个核心依赖（如 kline-charts-react → 自研图表）
- 决定在 MVP 中加入原本不在范围内的大模块（如用户系统）
- 产品方向出现重大调整

**触发条件：** 已确认的技术决策被推翻。

---

### 4. 产品方案大版本更新后

如果修改了 `项目文档.md`，增加了新的功能模块、改变了页面结构、调整了核心差异化，应该在修改完成后重新评审。

**触发条件：** 项目文档有实质性内容变更。

---

## 三、什么时候**不需要用** gstack

以下所有场景，**直接让 Claude Code 做就行，不需要走 gstack**：

### 开发和编码

| 场景 | 做法 |
|------|------|
| 初始化项目骨架（Docker / Vite / FastAPI） | 直接让 Claude Code 执行 |
| 写数据库表结构（SQLAlchemy models） | 直接让 Claude Code 执行 |
| 写 API 端点 | 直接让 Claude Code 执行 |
| 写形态匹配算法 | 直接让 Claude Code 执行 |
| 写前端页面和组件 | 直接让 Claude Code 执行 |
| 写单元测试 / 集成测试 | 直接让 Claude Code 执行 |
| 修 bug | 直接让 Claude Code 执行 |
| 重构代码 | 直接让 Claude Code 执行 |
| 性能优化 | 直接让 Claude Code 执行 |

### Git 操作

| 场景 | 做法 |
|------|------|
| 提交代码 (git commit) | 直接让 Claude Code 执行 |
| 创建分支 | 直接让 Claude Code 执行 |
| 查看 git log / diff | 直接让 Claude Code 执行 |

### 环境相关

| 场景 | 做法 |
|------|------|
| 安装依赖 (pip install / npm install) | 直接让 Claude Code 执行 |
| Docker 容器启动/调试 | 直接让 Claude Code 执行 |
| 数据库迁移 | 直接让 Claude Code 执行 |
| 环境变量配置 | 直接让 Claude Code 执行 |

### 日常问答

| 场景 | 做法 |
|------|------|
| "这个函数怎么写" | 直接问 Claude Code |
| "这段代码为什么报错" | 直接问 Claude Code |
| "A 方案和 B 方案哪个好" | 直接问 Claude Code，会给出建议 |
| "帮我解释一下这段逻辑" | 直接问 Claude Code |

---

## 四、一句话总结

```
gstack     = 阶段性工程评审（做之前先想清楚对不对）
Claude Code = 日常开发执行（想清楚了就动手做）
```

**项目文档改完了 → 跑一次 gstack 评审。评审通过后 → 找 Claude Code 写代码。代码写完了 → 找 Claude Code 写测试。需要改方案了 → 改完文档再跑一次 gstack。**

---

## 五、当前项目状态

当前处于「评审已完成，等待编码」阶段。**不需要再跑 gstack。**

下一步：告诉 Claude Code 从哪个模块开始实施即可。
